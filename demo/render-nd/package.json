{
  "name": "render-nd",
  "version": "0.1.0",
  "type": "module",
  "description": "Zero-dependency, isomorphic n-dimensional rendering library (geometry + projection + raster/WebGL backends).",
  "license": "SEE LICENSE IN LICENSE",
  "scripts": { "test": "node --test" }
}
