{
  "$comment": "Source of truth for the artwork on this repository's front page. Edit a sentence here and re-run tools/render_repo_art.py; tools/check_repo_art.py fails if the committed SVG stops matching.",
  "header": {
    "name": "telos",
    "role": "shared workbench for AI work",
    "tagline": "One workbench, and packets that recompute their own claims.",
    "words": ["assemble", "recompute", "derive", "witness", "replay"]
  },
  "flows": [
    {
      "file": "proof-lane.svg",
      "kicker": "one packet, end to end",
      "title": "How a claim about work becomes something a stranger can re-check",
      "alt": "Eight stages from loose materials to a replayable packet: fixture, assemble, completeness, recompute, join, derive, witness, replay. The verdict folds out of the checks rather than being read off the packet, so a canned pass embedded in the materials can never win. A witness that reports drift lowers the derived verdict, and a witness that cannot be reached is recorded as coverage lost rather than counted as a pass. Three outcomes: match when every claim recomputed, drift when a recomputed value disagrees with its claim, and unverifiable when the evidence to check a claim is not there.",
      "footnote": "A replay reads the packet. It does not re-run the work that produced it.",
      "stages": [
        {"title": "Fixture", "note": "Materials go in whole: sources, context, outputs, bytes."},
        {"title": "Assemble", "note": "One canonical packet, with no clock inside the hash."},
        {"title": "Completeness", "note": "A missing required field is named by its own path."},
        {"title": "Recompute", "note": "Each output digest is redone from the bytes it carries."},
        {"title": "Join", "note": "Every action is matched to the record that allowed it."},
        {"title": "Derive", "note": "The verdict folds out of the checks, and is never read."},
        {"title": "Witness", "note": "A second reader, whose absence is disclosed as absence."},
        {"title": "Replay", "note": "Verify dispatches on the schema the packet declares."}
      ],
      "returns": [
        {"from": 6, "to": 5, "label": "A WITNESS THAT REPORTS DRIFT LOWERS THE DERIVED VERDICT"}
      ],
      "outcomes": [
        {"label": "MATCH, EXIT 0", "note": "every claim recomputed from the packet", "tone": "verified"},
        {"label": "DRIFT, EXIT 1", "note": "a recomputed value disagrees with its claim", "tone": "drift"},
        {"label": "UNVERIFIABLE, EXIT 2", "note": "the evidence to check a claim is not there", "tone": "none"}
      ]
    }
  ],
  "cards": [
    {
      "file": "receipt-anatomy.svg",
      "kicker": "what you get back",
      "title": "One receipt, and how you would check each line of it",
      "source": "python tools/check_repo_art.py --json",
      "alt": "Six fields of a receipt the artwork checker emits, each with what comes back in it and how a reader would check that field for themselves. schema names the contract the record is written to. mode says whether the run rendered the files or only compared them. specs lists the diffable input the pictures are a function of. outputs carries one entry per drawing, each with a byte count and a SHA-256 read from the file on disk. checks carries one entry per gate, each naming itself and listing what it found. passed is the verdict, and it folds out of those checks rather than being written down, so a record cannot claim a pass its own checks do not support.",
      "footnote": "Hashes and byte counts are not drawn, because they change with the commit and a picture that shows one is wrong by the next. The shape of each field is drawn instead, and a gate holds every line here against a receipt the tool actually emits.",
      "fields": [
        {"key": "schema", "value": "project-telos.repo-art/v1", "tone": "none",
         "note": "The contract this record is written to. Pin it and a reader from a later version still knows what they are holding."},
        {"key": "mode", "value": "check", "tone": "none",
         "note": "check compares what is committed. render writes the files. The same code path, so the comparison cannot drift from the drawing."},
        {"key": "specs", "value": "1 entry", "tone": "none",
         "note": "The diffable input. Every picture is a pure function of this, so a review of the spec is a review of the artwork."},
        {"key": "outputs", "value": "3 entries", "tone": "none",
         "note": "One per drawing, each with the byte count and SHA-256 read off the file. Re-render and the two numbers have to come back equal."},
        {"key": "checks", "value": "16 entries", "tone": "none",
         "note": "One per gate, each naming itself and listing what it found. An empty list is a pass you can read rather than infer."},
        {"key": "passed", "value": "true | false", "tone": "verified",
         "note": "The verdict. It folds out of the checks rather than being written down, so a canned pass cannot enter the record."}
      ]
    }
  ]
}
