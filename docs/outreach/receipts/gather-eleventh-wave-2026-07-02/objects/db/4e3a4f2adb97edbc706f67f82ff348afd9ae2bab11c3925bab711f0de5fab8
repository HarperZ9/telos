# Pass 0148 Steelman

The strongest objection is that live query probes are still route tests, not research. A working endpoint can return weak, wrong, stale, or irrelevant records.

The settling test is a claim-extraction pass that turns a small subset of route records into claim cards, links them to source bodies or metadata, and rejects every card without a verifier, negative control, and replay path.
