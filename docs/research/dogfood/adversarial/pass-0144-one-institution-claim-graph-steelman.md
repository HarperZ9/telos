# Pass 0144 Steelman

The strongest objection is that a single institution graph can look rigorous while hiding scale problems: pagination, identifier drift, ambiguous affiliations, repository coverage gaps, rate limits, and license constraints.

The settling test is a multi-institution replay with the same contract: one positive identity join, one repository endpoint, one scholarly metadata sample, one dataset candidate, and one intentionally rejected ambiguous relation per institution.
