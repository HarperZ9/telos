{"falsification": "any package plan lacks a required field, requests install_now=true, lacks the pin policy, requires cloud credentials for import fixtures, lacks HTTPS sources, lacks import targets, lacks a fixture contract, or lacks a plan hash", "text": "Every pass 0021 package plan has QuantumFrameworkPackagePlan/v1 schema, install_now=false, pin_policy RESOLVE_IN_ISOLATED_ENV_THEN_FREEZE_WITH_HASHES, no cloud credentials required for import fixtures, at least one HTTPS source anchor, import targets, a first fixture contract, and a plan hash."}