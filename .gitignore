# OS / editor
.DS_Store
Thumbs.db
*.swp
*~

# Node
node_modules/
npm-debug.log*
*.tsbuildinfo

# Packaging artifacts (npm pack output; built by release.yml, not committed)
*.tgz

# Local / scratch (not part of the product)
*.local
.env
.env.*
.telos/
.playwright-mcp/
.warden-safe-cache/

# native-control local state (never commit credentials or queues)
vault.enc
*.vault.enc
queue.json
outreach-log.json

# personhood-forging surface: quarantined, owned-host red-teaming only.
# It is never published on the public path. Kept local; preserved privately
# out-of-tree. This fence stops it from ever being staged into this repo.
demo/native-control/redteam/

# Local-only Claude instructions (global standard: never commit)
CLAUDE.local.md

# Build output (global standard: never commit)
dist/
